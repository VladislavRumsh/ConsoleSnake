#ifndef RENDER_H
#define RENDER_H

void render(int rows, int cols, char** grid);
void buildGrid(int rows, int cols, char** grid);
void deAllocateGrid(int raws, char** grid);
void buildGriDAndRender(int rows, int cols, char** grid);


#endif 
